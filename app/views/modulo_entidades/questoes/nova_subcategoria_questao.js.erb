<% if @item_checklist.save %>
	$('#form_questao').html("<%= escape_javascript(render('form_questoes')) %>");
<% else %>
	alert('<%= @item_checklist.errors.full_messages.collect{|mensagem| "* #{mensagem}"}.join("\\n") %>');
<% end %>
