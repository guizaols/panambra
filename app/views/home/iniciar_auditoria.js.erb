<% if @retorno.first %>
	window.location.href = '<%= edit_entidade_auditoria_path(@retorno.last.unidade.entidade, @retorno.last, ordem: params[:ordem].to_s) %>';
<% else %>
	alert('<%= @retorno.last %>');
<% end %>
